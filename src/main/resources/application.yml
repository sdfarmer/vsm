server:
  port: 8080


spring:
  jpa:
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://120.48.82.2:3306/baoxian?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: seosem010
    hikari:
      max-lifetime: 0
    initialSize: 5
    minIdle: 10
    maxActive: 1000
    #配置获取连接等待超时的时间
    maxWait: 60000
    #配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
    timeBetweenEvictionRunsMillis: 60000
    #配置一个连接在池中最小生存的时间，单位是毫秒
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1
    #指明连接是否被空闲连接回收器(如果有)进行检验.如果检测失败,则连接将被从池中去除.
    testWhileIdle: true
    #指明是否在从池中取出连接前进行检验,如果检验失败,则从池中去除连接并尝试取出另一个
    testOnBorrow: true
    #指明是否在归还到池中前进行检验
    testOnReturn: false



  #  mvc:
  #    static-path-pattern: /static/**
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    #    suffix: .html
    mode: HTML
    encoding: UTF-8

# 数据库配置
mybatis:
  type-aliases-package: pojo
  mapper-locations: classpath:mybatis/mapper/*.xml


# 日志配置
logging:
  level:
    # web日志
    org.springframework.web: info
    # security 日志
    org.springframework.security: error
    # mybatis日志
    org.mybatis: info
  charset:
    # 输出控制台编码
    console: utf-8
    # 输出文件编码
    file: utf-8
  # 输出文件名及路径，不配置则不输出文件
  file:
    # 切记，该文件表示正在产出日志的日志文件。并不会打包，当文件大于max-file-size,会根据file-name-pattern格式打包
    # 名称为log/ota.log文件夹会在项目根目录下，打包后会在启动包同目录下；名称为/log/ota.log的文件夹会在项目所在磁盘的跟目录下
    name: log/VSM_NEW.log
  logback:
    rollingpolicy:
      # 单文件的大小，默认10M, 超过之后打包成一个日志文件
      max-file-size: 1MB
      # 日志保存的天数
      max-history: 30
      # 打包文件格式,默认： ${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz,书写格式为：文件路径/文件名.%i.文件后缀,其中%i不可省去，否则无日志显示
      # 例如： 日期为：2021/11/5 ,则打包文件之后为： log/ota.2021-11-05.0.gz,0表示日志的第一部分，后续就是，1,2,3...
      # 如果是压缩包，里面会多一个名log/ota.2021-11-05.0的日志文件
      # 如下面的例子，打包之后为: log/2021-11/ota.2020-11-5.0.log，这是一个日志文件
      file-name-pattern: log/%d{yyyy-MM}/ota.%d{yyyy-MM-dd}.%i.log
#logging:
#  file:
#    path: log
#    name: vsm_ai.log
#  charset:
#    file: utf-8